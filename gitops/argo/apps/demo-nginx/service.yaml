apiVersion: v1
kind: Service
metadata:
  name: demo-nginx
  namespace: demo-nginx
  labels:
    app: demo-nginx
spec:
  ports:
    - port: 80
      targetPort: 80
      nodePort: 32080
      protocol: TCP
  selector:
    app: demo-nginx
  type: NodePort